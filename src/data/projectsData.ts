// Minimal definition to satisfy imports after file deletion
// Actual string values might differ from original, but keys are based on usage.
export enum ProjectStatus {
  ACTIVE = 'active',
  DEPLOYED = 'deployed',
  FIELD_TESTING = 'field-testing',
  OPERATIONAL_PILOT = 'operational-pilot',
  DEVELOPMENT = 'development',
  CONCEPT = 'concept',
  // Add other potential statuses like MAINTENANCE if needed
  MAINTENANCE = 'maintenance', 
} 